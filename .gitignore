# ── Xcode ────────────────────────────────────────────────
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist
xcuserdata/
*.xcuserstate
*.xccheckout
*.xcscmblueprint
build/
DerivedData/
*.ipa
*.dSYM.zip
*.dSYM

# ── Swift Package Manager ────────────────────────────────
.build/
.swiftpm/

# ── CocoaPods ────────────────────────────────────────────
Pods/

# ── Certificates & Provisioning (절대 커밋 금지) ─────────
*.p8
*.p12
*.cer
*.certSigningRequest
*.mobileprovision
*.provisionprofile
ExportOptions.plist

# ── Secrets / API Keys (절대 커밋 금지) ──────────────────
.env
.env.*
secrets.xcconfig
APIKeys.swift
APIKeys.plist
GoogleService-Info.plist

# ── macOS ─────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
Icon\r
._*
.Spotlight-V100
.Trashes

# ── 프로젝트 내부 기획 문서 ──────────────────────────────
*.pdf

# ── Claude ───────────────────────────────────────────────
CLAUDE.md

# ── Firebase Functions ───────────────────────────────────
# 빌드 산출물(lib/)과 의존성(node_modules/, 89MB)은 src/*.ts 에서 다시 만들어진다
lolive-firebase/functions/node_modules/
lolive-firebase/functions/lib/
lolive-firebase/.firebase/
